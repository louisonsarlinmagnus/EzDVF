name: .NET

on:
  push:
    branches: [ deploy ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Création des binaires
      run: dotnet publish -c Release -r linux-x64 --self-contained true -p:PublishSingleFile=true /p:PublishTrimmed=true

    - name: Il est ou le Youki
      run: |
        echo "Dans le premier EZDVF"
        cd /home/runner/work/EzDVF
        ls
        echo "Dans le /net5.0"
        cd /EzDVF/bin/Release/net5.0
        ls
        echo "Dans /linux-x64/publish/"
        cd /linux-x64/publish/
        ls
